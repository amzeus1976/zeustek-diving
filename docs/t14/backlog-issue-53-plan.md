# Issue #53 — Stage 9 finishing plan and execution ledger

The concrete `IMPLEMENTATION PLAN` was posted to [issue #53](https://github.com/amzeus1976/zeustek-diving/issues/53#issuecomment-5831985152) after a read-only source and live UI audit. This file keeps the checkpoint recoverable locally.

## Baseline

- Starting source: `d581ebfd9e6ca475c8256f7e0dd84952de2727fc` on `t14/master-forward-build`; issue branch `t14/issue-53-stage9`.
- Production: app1.0.58 / Sites130, deployment `appgdep_6ab65bed90e08191a67d888e89f41378`, saved source `d581ebfd9e6ca475c8256f7e0dd84952de2727fc`.
- Current accepted release for rollback: app1.0.58 / Sites130, deployment `appgdep_6ab65bed90e08191a67d888e89f41378` (reverify immediately before release). Sites129 is historical, not the first rollback choice.
- Read-only owner snapshot: 6,400 canonical records, including 66 Dives and one Gas Plan; private ordered-content fingerprint evidence is held outside Git. No owner writes are planned.
- GitHub main at audit: `ba86e49c21d84a0af434387c9133de9092720750`; no open PR.

## Confirmed tasks

1. Remove the bulky household card from the sidebar, retain an accessible version/changelog link and cloud-status indicators. Check mobile and desktop navigation.
2. Correct Bibliography editor terminology and suppress its observed false empty state while canonical media loads. Leave `Dive Media` route and record kind for compatibility.
3. Replace the Technical Diving `T05 loadouts` internal-stage copy with a real owner action.
4. Label the mobile menu close button and verify keyboard/focus behavior; preserve the already-correct `inert` closed sidebar.
5. Inventory remaining substantive multi-field dialog editors; migrate in bounded groups to `RecordEditorWorkspace` without changing persistence or treating read-only details as editors.
6. Align the Overview `Open people` action label to the canonical `People` name.

## Gates

Focused verification per edit, full regression, typecheck, production build/PWA, privacy, targeted lint, version/cache and all nine protected calculation hashes. Check navigation, Bibliography, Technical Diving, converted editors, no console errors, no broken assets and no page overflow at 390/820/1024/1440. Compare fresh owner IDs/content before and after production. Release one exact candidate, smoke it read-only, then reconcile accepted source to GitHub. No Gmail sync.

## Progress

- 2026-09-25: read-only audit complete; implementation plan posted; issue branch created clean from accepted Sites130 source. No application edits yet.
- 2026-09-25: first bounded implementation group: sidebar card removed while version link remains; Bibliography loading/error state and terminology corrected; technical empty-state copy corrected; mobile menu close named; Site and Trip complex editors moved to in-route workspaces. `EditorSections` now supports both legacy dialogs and in-route workspaces. Site/Trip create and edit opened locally against isolated fixture data; dirty drafts prompted before exit and were discarded without saving. Bibliography/Overview/Trips/Sites browser routes were checked locally. Focused editor tests 10/10 and Trip/dialog tests 7/7; full regression 132 files / 763 tests PASS; typecheck PASS. No production writes or deployment. Remaining: inventory/migrate other complex editors and run the final release gate, including four viewport widths, privacy, build/PWA, protected hashes and fresh owner comparison.
- 2026-09-25: Dive Log create/edit editor converted to the same in-route workspace. Local fixture browser confirmed eight jump sections, no false dirty state from section navigation, correct `Me` identity, dirty-draft confirmation, and no page-level overflow at 390 and 820. The old Dive dialog assertion now verifies the workspace contract. Full regression remains 132 files / 763 tests PASS; typecheck PASS. No fixture or owner record was saved. `components/shared/record-editor-workspace.tsx` targeted lint passes; the pre-existing `react(no-children-prop)` finding in `tests/accessible-dialog.test.ts` remains unrelated lint debt.
- 2026-09-25: Technical Diving's versioned reference form moved to the route workspace. Required-field gating and HTTP(S) source URL validation remain, while save failures now surface through the workspace without losing the draft. Focused technical tests 13/13, typecheck and targeted component lint PASS. Local 390px fixture check showed no overflow and confirmed dirty Cancel protection; no reference was saved.
- 2026-09-25: Conservation activity and programme capture moved to in-route workspaces. Both rendered locally at 390px without overflow; activity edits prompted before discard and the programme capture stayed disabled until required fields were supplied. Focused conservation/dialog tests 6/6, full regression 132 files / 764 tests and typecheck PASS. The existing conservation component lint findings at unrelated effect/ARIA lines remain pre-existing debt; no new lint finding was introduced. No local fixture or production record was saved.
- 2026-09-25: Skill Catalogue single/bulk/CSV editing moved to route workspaces; existing creation/import persistence remains. Single and bulk local drafts prompted on Cancel; CSV is gated until a valid preview and selected rows exist. At 390px all three rendered without page overflow. Focused Skill/dialog tests 17/17, full regression 132 files / 764 tests and typecheck PASS. Existing Skill component effect/ARIA lint findings remain; editor `autoFocus` was removed to preserve workspace focus behavior. No file was imported and no record was saved.
- 2026-09-25: reusable Loadout and physical Cylinder create/edit forms moved to route workspaces without changing their canonical stores or calculations. Local 390px checks showed correct sections, no overflow and dirty Cancel protection; no fixture was saved. Focused Loadout/Cylinder tests 24/24, typecheck and targeted component lint PASS. The multi-action Cylinder detail and Apply Loadout dialogs remain for separate classification; do not mechanically migrate their inline action forms.
- 2026-09-25: Calendar event create/edit moved to a route workspace with required name/start date and end-date order gating. Focused planning tests 18/18, typecheck and targeted component lint PASS. Local 390px create draft showed no overflow and prompted before discard; no event was saved. The previous checkpoint's full regression was 132 files / 765 tests and production build/PWA completed successfully. These are interim checks, not the final frozen candidate gate.
- 2026-09-25: Computer import review and post-import Dive linking moved to route workspaces. The import commit remains gated to its review summary and durable evidence store; optional field decisions stay explicit. Focused import tests 16/16, full regression 132 files / 765 tests, typecheck and targeted component lint PASS. Existing local profile fixtures allowed a 390px link/cancel check with no overflow and dirty-decision confirmation. The repository's synthetic UDDF fixture was staged in the isolated local account, and the wizard showed two new profiles, a disabled commit on Preview, an enabled commit only on the Finish step, and no overflow. The wizard was cancelled without committing. No link, import or production owner record was saved.
- 2026-09-25: Dive detail's Overview/Debrief/Story and the separate Skill Evidence create/edit form moved into in-route workspaces. Debrief/Story retain their existing queued auto-save and wait-for-save navigation behavior; Skill Evidence keeps its explicit save, validation and draft-discard protection. Focused Dive perspective/view tests 17/17, full regression 132 files / 765 tests and typecheck PASS. Isolated 390px browser check opened an existing local fixture Dive, entered Skill Evidence, verified the changed draft confirmation, cancelled back to the same Debrief and saw no page overflow. No evidence or Dive record was saved. The production build and four-width final gate remain pending for this changed source.

## Remaining editor inventory and classification

- Remaining `AccessibleDialog editable` surfaces: Apply Loadout is an assignment operation to an existing Plan/Trip/Dive, not the canonical Loadout editor (which is already in-route); Cylinder detail is a multi-action viewer for fills, analyses and usage while its physical-record editor is already in-route. Media bulk caption/category editing and photo correction are attachment operations used inside several read-only details. Classify each action against the Stage 9 editor policy; preserve atomic writes and upload/retry semantics. Do not convert a multi-action view mechanically merely to remove the dialog element.
- Read-only details, media lightboxes and destructive confirmations may remain dialogs. The synthetic fixture-review workflow is test infrastructure and is not owner-facing Stage 9 polish.
- Classification: Apply Loadout is an atomic assignment action, while Cylinder detail groups fill, analysis and usage operations around a read-only view. Media bulk edits and photo correction are attachment operations with upload/retry semantics. These are not canonical multi-field record create/edit forms; keeping their focused dialogs preserves existing atomic and retry behaviour under this bounded Stage 9 issue. The canonical Loadout, physical Cylinder, Dive and other record editors above use the in-route workspace. No routine complex record editor remains knowingly deferred under this issue.
- Exact release candidate gate on the staged app1.0.59 source: 132 files / 766 regression tests PASS; typecheck PASS; production build/PWA PASS (427 precache entries, 42,295.77 KiB; complete icon library not eagerly cached); focused touched Dive editor lint PASS; six-credential client-bundle privacy scan PASS; nine protected calculation hashes unchanged; isolated Dive/Gas Plan fixtures unchanged. Browser checks at 390/820/1024/1440 on isolated fixtures show no page-level overflow, broken images or application console errors across the touched routes and Dive editor. A production-built local shell starts, but its Sites identity callback is unavailable outside Sites, so authenticated local browser checks used isolated Vite fixtures; live smoke remains mandatory. Fresh read-only Sites130 backup: 6,400 canonical rows, including 66 Dives, one Gas Plan, 11 People and five Operators; aggregate ordered-content fingerprint `198e5ab30d999d30` (private per-ID comparisons kept out of Git).
- Historical next checkpoint for the initial app1.0.59 candidate (superseded by the review correction below): release once, verify live, then reconcile GitHub. No Gmail sync.

## Sites131 release attempt and review correction

- Candidate `b31f0e32064e0067772243161ac4bf8717a6323a` was saved as app1.0.59 / Sites131 and deployed as `appgdep_6ab67562d4648191bf9852a7beafcf52`. Initial live route, four-width, console, asset and 6,400-record per-ID checks passed. GitHub PR #65 was opened but not merged.
- Codex PR review identified a P1 error path: the Loadout cards stayed interactive above an active workspace. Selecting another Loadout could change the editor's `item` prop while React retained the first record's draft and `entityId`, risking an overwrite on Save. This invalidated release acceptance despite the initial smoke.
- Production was restored immediately to the verified app1.0.58 / Sites130 source `d581ebfd9e6ca475c8256f7e0dd84952de2727fc` through successful recovery deployment `appgdep_6ab6776076908191ab16bdbc7a33de5e`. The live version link reads v1.0.58. GitHub main was not changed. No production owner records were written by the agent.
- Corrective design: an active Loadout or physical Cylinder editor exclusively owns its route; list and record-switch controls are unmounted. The editor is keyed by canonical record identity. A focused test was red before the fix and now passes; an isolated 390px browser check confirms the other record and New controls are absent, one editor is visible, Cancel prompts for dirty drafts, and discard returns to the list without saving or overflow.
- Retry candidate uses app1.0.60 and new navigation/static cache identifiers. Re-run the complete exact-source release gate, capture a fresh owner baseline, and reverify the accepted Sites130 recovery target before the retry. Update PR #65 only after the corrected candidate is committed; do not merge or close #53 until successful production verification. No Gmail sync.
- Corrected candidate local gate: 132 files / 767 tests PASS; typecheck PASS; targeted Loadout/Dive/workspace/release test lint PASS; production build/PWA PASS (427 precache entries, 42,295.96 KiB, complete 748-icon library excluded). At 390/820/1024/1440 the isolated Loadout editor shows exactly one workspace, no other-record/New controls, no broken visible images and no page-level overflow; a dirty draft prompted on Cancel and was discarded without saving. Local application console errors: zero. Six-credential bundle/staged-source privacy scan PASS with zero matches; local owner fixtures unchanged (76 rows, SHA-256 `221e9ff72ab5deb482b5ef0029e556c8ee7a3e667e54ff52e0671ca646411a10`); all nine protected calculation hashes PASS. Fresh production owner baseline and recovery status must be rechecked before publishing.

## Second PR review and app1.0.61 correction

- The review of `873061b` identified five more P1 paths: Site, Trip, Skill, Calendar and Conservation lists could remain interactive beneath active editors. A second record selection could reuse the first editor draft and save to the wrong record. Sites132/app1.0.60 was archive-backed but not deployed.
- Focused isolation tests were written and failed on all five routes. Application commit `4d88ba25905b3c064dc649b7d0b318f8e6d59aed` makes each editor exclusive to its route/record list and keys it by canonical ID. The Settings Skill Catalogue is nested in a collapsible card; a separate red test and local browser check led to guarding collapse through the existing discard policy.
- App1.0.61 local release gate: 133 files / 774 tests PASS; typecheck PASS; production build/PWA PASS (427 entries; full icon library excluded); version/cache test PASS; six-credential privacy scan PASS; 10 protected-calculation checks PASS with all nine frozen hashes byte-identical; 76-row isolated owner fixture fingerprint unchanged. Broad lint in legacy files still has 102 pre-existing findings, but the staged diff introduces zero findings on changed lines; the new test, shared card, service worker and changelog lint cleanly. At 390/820/1024/1440, Site, Trip, Skill, Calendar and Conservation editors each opened with no active record-switch list, no overflow and no console error. Incomplete lazy navigation icons were not broken: completed failed images were zero and local asset response was HTTP 200. No production writes or Gmail sync.
- Next: push PR #65 update and obtain clean review; save app1.0.61 to a **new** Sites version, recheck rollback and owner baseline, deploy once, live-smoke and compare every owner record, then reconcile GitHub main only if accepted. If review or live verification fails, retain Sites130 as production and preserve the branch/evidence.
