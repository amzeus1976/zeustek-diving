# T14 owner identity and 31-route navigation candidate

## Sites127 attempt, finding and verified recovery

Sites127/app1.0.55 was saved from source `6f608915f49239a8bec72f90a0feb42ec5545543` and deployed once as `appgdep_6ab530e85dac81919afb47ea92f2dc5c`. Live read-only smoke confirmed all 31 supplied icons and one owner Team candidate, but the `Me` label showed **Rescue Diver** while the owner’s canonical Overview showed **Master Scuba Diver**. This was a release-blocking display error; no GitHub accepted-source push followed. The saved Sites126/app1.0.54 version was restored as `appgdep_6ab5315a23f48191813715f6a6a02a59`; the deployment succeeded and a fresh live URL showed v1.0.54. The existing Sites source repository retains the candidate commits for recovery. No Dive draft was saved and no Gmail sync was run.

Read-only award inspection established the root cause: the actual Master Scuba Diver certification is categorised `other`, while the previous `Me` helper excluded `other` before ranking the owner’s awards. The older owner-entered Person summary also contains a misspelling; it is preserved unchanged. Corrected application checkpoint `408df5a9c75b9e555945a22c51fde30e993f4208` uses the existing award-priority ordering across all owner-associated awards for display and keeps profile-derived fallbacks. It does not alter certification categories, Person fields or protected calculation files. The red fixture reproduced Rescue Diver instead of Master Scuba Diver; after the fix, focused owner/navigation tests are 11/11 and the full suite is **131 files / 753 tests PASS**. Typecheck, production build/PWA, focused lint, six-credential privacy scan and nine protected hash checks pass. A test-only local owner Person then rendered exactly one `Me · Master Scuba Diver` candidate and no duplicate ordinary Person candidate; its unsaved Dive draft was cancelled. The local test fixture is not production data.

App/cache identifiers are advanced to 1.0.56, navigation-v6 and static-v5 for the corrected retry. Before another Sites save/deploy, commit this evidence, rebuild/package exact HEAD, recheck Sites126 rollback and capture a fresh per-ID production snapshot. Retain the failed Sites127 result in the release ledger rather than reclassifying it as passing.

The corrected PWA service worker references all 31 runtime navigation derivatives, zero master paths and 17 selectively used complete-library icons; it does not precache all 748 complete-library images.

The corrected local browser fixture now shows one `Me · Master Scuba Diver` and no duplicate owner at **390, 820, 1024 and 1440**; all four Dive editor widths have zero page-level overflow and zero application console errors. A fresh authenticated, read-only Sites126 backup after restoration contains **6,399 exportable active canonical records**, including 66 Dives, one Gas Plan, 11 People and four Operators. It excludes the Gmail connection secret (zero matching rows). Sorted per-ID content fingerprints produced aggregate `e6cfc662b8ed1103` using FNV-1a 64 over `[id,kind,dataJson,createdAt,updatedAt]`; this is the immediate pre-retry canonical-data comparison point. The server table also contains the separately protected encrypted connection row, which must remain preserved but never exported.

## Preserved source and correction

Application checkpoint `a38d6a29a7bf7ad6771fe349771a907fb7160b53` on `t14/master-forward-build` extends the completed People / Dive Entity correction at `c2c9aad0`; it does not replace that architecture. The duplicate owner came from the Dive editor rendering a hard-coded `self` checkbox and every Person, including the canonical owner Person. The same raw IDs were used for team, buddies and leader. A read/edit projection now resolves the existing owner-profile flag, renders one `self` candidate enriched by certification evidence, excludes that Person from other candidates and maps legacy owner Person IDs to `self`. Future Dive saves use `self`; historical Dives are not bulk-rewritten. Owner-entered `highestQualification` is left untouched.

The authoritative archive `navigation_icons_all_batches.zip` has SHA-256 `ed344fe658f57a47cccda650a7b382f872f79911a0cf8208f02f835becc3daa3`. Its 31 supplied 1254×1254 RGBA PNGs are retained byte-identical under `assets/navigation-icons/master/` (33,409,859 bytes). The reproducible importer creates 31 transparent 128×128 navigation derivatives under `public/brand/icons/navigation/` (687,212 bytes) and a hashed manifest. The existing curated and 748-icon registries remain intact. The 31 route images are accessible through visible text labels, with `aria-current` active state and no generic route fallback. Masters are outside the public/PWA asset directory.

## Exact local release gate

- Focused owner/navigation tests: 9/9; full regression: **131 files / 751 tests PASS**; typecheck PASS; production build PASS.
- PWA build for the first candidate: 429 precache entries, including 31 runtime route derivatives and zero master paths; navigation-v5 and static-v4 cache identifiers. The complete 748-icon library was not eagerly cached. App version `1.0.55`.
- Focused new/changed-module lint PASS. The older dashboard monolith retains pre-existing broad-lint findings, already documented in prior T14 evidence. No new finding was introduced in the isolated new files.
- Client-bundle/staged-diff privacy scan: six private credential fingerprints, zero matches. Nine approved protected calculation files: zero hash mismatches. Local 75-Dive/one-Gas-Plan fixture hash remains `221e9ff72ab5deb482b5ef0029e556c8ee7a3e667e54ff52e0671ca646411a10`; Plan hash remains `edeffbe5e65a30ac2eaa54f36562ee50e754ea53f90c9ef00d468cff2f39bebe`.
- Browser at 390/820/1024/1440: all 31 route images loaded at natural width 128, no broken image, zero page-level horizontal overflow and zero application console errors. People, Dive Centres, Insights and Logbook routes rendered. Mobile/tablet menu, active route, Team-only buddy choices and leader choices were exercised. Existing local Dive edit/create drafts were cancelled. The circular photo editor used a disposable local image: 1:1 circle, zoom, drag/recentre and Reset worked; crop and Person drafts were cancelled. The local fixture has no owner Person, so owner-specific enrichment is covered by focused tests and remains a live read-only acceptance check after deployment.
- Production-built Wrangler smoke in isolated local state: 200 for People/Insights route PNGs, service worker and manifest; unauthenticated owner APIs return 401. The shell redirects to sign-in as expected. No local or production owner record was created for this gate.

## Fresh production protection snapshot

Read-only Sites126 source `14a5fe8540bf6e6acc664a3ccf71df4982ae2b04`, deployment `appgdep_6ab4624dfb28819193f77caf20388762`, is the current accepted release and rollback target. A fresh read of all `dive_records` rows found 6,610 total rows, 6,400 active; 66 active Dives, one active Gas Plan, 11 active People and four active Operators. A sorted per-ID content fingerprint map and aggregate `6032e8213886ba23` (FNV-1a 64 over complete row fields, including timestamps and deletion state) were captured without printing owner values. The aggregate/count method and kind counts are preserved in ignored private evidence `work/t14-evidence/owner-identity-navigation-pre.json`. These are a fresh snapshot, not historical target counts; compare actual IDs and row hashes again immediately after publication and account for legitimate owner activity.

The prior saved Sites124 release remains an additional recovery artifact. The immediate rollback target for this candidate is the **currently accepted Sites126**, once its health and saved artifact are reconfirmed at deployment. Gmail sync remains disabled and its single failed acceptance result remains `FAILED — upstream_failure`, owner-approved release exception. No new live Gmail operation is authorised.

## Next exact steps

Freeze the documentation checkpoint and confirm clean source. Package the build output from that exact commit; push exact source to the existing Sites source repository; save and deploy one new Sites version after confirming the pre-deployment owner snapshot and Sites126 rollback. Read-only production smoke must verify version/source, all 31 icons, People/Entities, owner once as Me with qualification, Team/Buddy/Leader legacy projections, crop editor, PWA and exports/privacy, no errors/overflow, disabled Gmail, protected hashes and per-ID owner data. On failure restore verified Sites126 and keep the candidate off accepted GitHub main. On success reconcile that exact accepted source through the established GitHub PR/main procedure, then record deployment and source-match evidence.
